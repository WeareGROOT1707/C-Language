#include<stdio.h>

int main()
{
    int marks ;
    printf("Input the marks:\n");
    scanf("%d",&marks);
    if(marks>90)
    {
        printf("A+");
    }
    if(marks>=80&&marsk<=90)
        {
            printf("A");
        }
        if(marks>=70&&marks<=80)
        {
            printf("B");
        }
        }
    else
    {
        if(marks>=70&&marks<=80)
        {
            printf("Eligible");
        }
        else
        {
            printf("Not Eligible");
        }
        }
    }
    return 0;
    }
